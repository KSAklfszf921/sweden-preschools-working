# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-publishable-key

# Required for production deployment
# These should be set in your deployment platform's environment variables
# Not needed for local development as they are handled by Supabase Edge Functions

# For GitHub Pages deployment, these are handled via Edge Functions:
# - MAPBOX_TOKEN (set in Supabase Edge Function secrets)
# - GOOGLE_GEOCODING_API_KEY (set in Supabase Edge Function secrets)
# - SUPABASE_SERVICE_ROLE_KEY (set in Supabase Edge Function secrets)